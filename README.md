# rustUI
A demo for rust development in debian 12

Iced UI: https://github.com/iced-rs/iced

# Poppler depends on below 2 packages
sudo apt update
sudo apt install libpoppler-cpp-dev  # 对于C++绑定
sudo apt install libpoppler-glib-dev # 对于Glib绑定

